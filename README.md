
WIODv2
======

A simple function to download the newly available World Input-Output
Database (WIOD). 

The function `getWIODv2` can download and import a WIOT for a specific
year, in three precomputed formats: wide, long and as a list of
matrices.
